subprojects {
    apply plugin: 'idea'
    apply plugin: 'java'

    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11

    ext {
        guice = '4.2.2'
        log4j2 = '2.12.1'
    }

    repositories {
        mavenCentral()
    }
}
